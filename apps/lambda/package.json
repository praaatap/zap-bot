{
    "name": "meeting-processor",
    "version": "0.1.0",
    "description": "AWS Lambda function for processing Zap Bot meetings: AI summarization, transcript analysis, action item extraction",
    "private": true,
    "type": "module",
    "main": "index.mjs",
    "scripts": {
        "build": "node build.mjs",
        "zip": "node build.mjs && cd dist && zip -r ../function.zip .",
        "test": "node test.mjs"
    },
    "dependencies": {
        "@aws-sdk/client-bedrock-runtime": "^3.750.0",
        "@aws-sdk/client-s3": "^3.750.0",
        "@pinecone-database/pinecone": "^7.1.0"
    }
}